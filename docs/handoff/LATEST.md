# Latest Handoff

## Current Pointer

The latest relevant continuity notes:

1. **`docs/handoff/2026-04-28-exp4-stuck-recovery.md`** — Recovered the platform from a simultaneous Exp 4 *stuck-report* and dead-frontend outage. Two distinct bugs: (a) `_run_exp4_full_experiment` failed with `Cannot write to closing transport` because the WebSocket dropped during the 10–20 s synchronous executor and the handler had no fallback delivery path; (b) Vite crashed with `Error: read EIO at TTY.onStreamRead` because `launch.sh` left its stdin attached to the parent TTY. Fix is three-fold: server now caches the rendered payload on `self._exp4_report_cache`, broadcasts via a transport-tolerant `_safe_ws_send` helper, and exposes a new `fetch_exp4_report` WS handler; the frontend auto-requests the cached report on every reconnect and polls it every 8 s while waiting; `launch.sh` now starts Vite with `< /dev/null` so the launcher's TTY can never crash it. End-to-end WS smoke test confirms a forced mid-pipeline disconnect now recovers cleanly.
1. `docs/handoff/2026-04-27-architecture-cleanup.md` — Project-wide cleanup pass. Collapsed the historical "everything in the repo root" layout into a single canonical structure: removed ~50 dead/duplicate root files (legacy `expt*_sim.py` standalone scripts, Gradio `expt*_ui.py`, drafts, `__pycache__`, duplicate PDFs, unused `server/` and `utils/` packages, `cloudflared` binary, the old PPT generator). Reorganised reference materials (`phy1002/` → `docs/reference/phy1002/`, `实验5/` → `docs/legacy/exp5_classmate_code/`). Fixed a broken `run.py` registry entry pointing at a deleted module, removed a dead `CAMERA_SCRIPT_DIR` import in `core/webrtc_server.py`, rewrote `README.md` and `AGENTS.md` and `state/artifact_manifest.json` to match reality, added `camera/README.md` clarifying those scripts are dev-time only, and renamed `LabReportPDF.tsx` → `Exp1ReportPDF.tsx` for consistency with the other six lab-report components. Verified end-to-end: every Python file still AST-parses, `npx tsc --noEmit` is clean, `npx vite build` succeeds, and the live frontend + Isaac Sim + WebRTC + WebSocket services are still running.
2. `docs/handoff/2026-04-27-stability-hardening.md` — Pipeline-wide connection stability hardening. Backend (heartbeat, parallel telemetry fan-out with per-client timeout, video-track watchdog, multi-STUN, dead-client sweeper, error-isolated WS handlers) + frontend WS service (full reconnect state machine with exp backoff, 15 s heartbeat, 30 s stale watchdog, replay `enter_experiment` on reconnect, `RECONNECTING` status) + WebRTC viewer (two-stage ICE recovery — restartIce then full reconnect; 6 s stall watchdog; periodic WS-JPEG → WebRTC upgrade) + ExperimentView (three-state badge, post-reconnect parameter resync). Users no longer need to refresh after a network blip; slow clients can no longer stall the lab. See `docs/adr/ADR-0004-stability-hardening.md` for the full decision record.
3. **`docs/handoff/2026-04-27-exp4-react-pdf.md`** — Experiment 4 lab-report PDF rebuilt with `@react-pdf/renderer` (the same toolchain Exp 1 / 2 use) so the visual style now matches Exp 1's Lab_Report_Angular_Momentum.pdf exactly: CUHK-Shenzhen cover page, Times-Roman body, codecogs-rendered LaTeX equations, academic top/mid/bottom-rule tables, fixed running header, page numbers, 15-page Objective / Method / Raw Data / Data and Error Analysis / Conclusion structure with full instrumental error propagation (electronic balance, vernier caliper, manufacturer kappa) and answers to all six Expt_4.pdf questions. The matplotlib PDF is kept inside the ZIP as an offline-readable backup. New `frontend/src/components/Exp4ReportPDF.tsx`; `ExperimentView.tsx` adds an `exp4GeneratePrettyPDF` callback and the Download PDF Report button now invokes it.
1. `docs/handoff/2026-04-27-exp4-report-export.md` — first iteration: Experiment 4 (Driven Damped Harmonic Oscillations) gained a "Generate Lab Report" button. New `core/exp4_report.py` runs RK4 ringdown + 3-damping resonance sweep + 3-regime phase comparison entirely in Python, computes a damped-sine LM fit, half-power FWHM, half-amplitude asymmetry index, and a linear-LS sinusoid phase fit (within 0.1° of theory). Backend ships base64 plots/CSVs/JSON/Markdown/ZIP through `exp4_progress` / `exp4_report_ready` WS messages.
1. **`docs/handoff/2026-04-27-exp5-report-redesign.md`** — Experiment 5 lab report PDF rebuilt with `@react-pdf/renderer` to match Exp1 typography. Backend now ships only summary data + 4 base64 matplotlib PNGs; the browser composes the formal PHY1002 report (cover page, sections, LaTeX equations via codecogs, horizontal-line tables, figure captions). Replaces the earlier ugly Python-side `PdfPages` output.
1. `docs/handoff/2026-04-27-exp5-report-export.md` — Original Experiment 5 export pipeline (server-side telemetry recording + Python report generation + WS handler + frontend export button + 30 s timeout safety). Superseded by the redesign above for the visual layout, but the data-flow notes still apply.
1. **`docs/handoff/2026-04-24-exp6-centripetal-force.md`** — Experiment 6 (Centripetal Force) replaces its stub with a PhysX-native integration: kinematic rotor spun about +Z by an async pose-driver, dynamic bob attached via a USD PrismaticJoint with linear DriveAPI (stiffness=k, damping=c, target=r) that acts as the spring. The centripetal force read on the UI is the real spring force PhysX computes as it integrates the bob's motion, cross-checked against m·v²/r from PhysX-measured speed and radius. Frontend unlocked; mass, target radius, ω, spring stiffness, damping, plus Start/Stop/Reset.
2. `docs/handoff/2026-04-24-public-share-tunnel.md` — New `share.sh` script exposes the frontend as a public HTTP URL (`http://bore.pub:<PORT>`) via the bundled `launchers/bin/bore` client so teams on other networks can use the experiments without SSH access or campus port forwarding. Includes HTTPS fallback via localhost.run (`./share.sh --via lhr`) and gotchas for future agents (do not curl from inside the container; new URL every restart).
3. `docs/handoff/2026-04-17-exp4-driven-damped.md` — Experiment 4 (Driven Damped Harmonic Oscillations) integrated end-to-end: procedural PhysX torsional oscillator (RevoluteJoint + DriveAPI), resonance + phase-lag telemetry, free-oscillation ringdown mode, unlocked frontend.
3. `docs/handoff/2026-04-17-exp3-ballistic-pendulum.md` — Experiment 3 (Ballistic Pendulum) integrated end-to-end: procedural PhysX scene, live WebSocket telemetry, unlocked frontend, and batch-mode v₀ sweep with Markdown report.
4. `docs/handoff/2026-04-17-exp5-physical-pendulum.md` — Experiment 5 (Rotational Inertia / Physical Pendulum) integrated from teammate's script; unlocks full web interactive flow for exp5.
5. `docs/handoff/2026-04-10-vr-integration.md` — VR hand tracking integration (Meta Quest 3S → WebRTC server).
6. `docs/handoff/2026-03-29-session-context-anchor.md` — short "what we're doing now" (paths, ports, browser/Isaac workflow).
7. `docs/handoff/2026-03-29-fullstack-integration.md` — full-stack integration narrative.

Previous notes:

- `docs/handoff/2026-03-28-startup-workflow-upgrade.md`
- `docs/handoff/2026-03-28-context-foundation.md`

## Update Rule

Whenever a new handoff supersedes this one, update this file in the same change set.
